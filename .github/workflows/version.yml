name: Versions

on: 
  push:
    tags:
    - '*'

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: version
      uses: ncipollo/semantic-version-action@release/v1
      with:
        tag: "0.21.0"
    - name: Dump steps context
      env:
        STEPS_CONTEXT: ${{ toJson(steps.release.outputs) }}
      run: echo "$STEPS_CONTEXT"